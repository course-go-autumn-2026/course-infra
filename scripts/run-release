#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
"$root/scripts/build-release" && "$root/scripts/audit-release-cleanup" || {
	status=$?
	rm -rf "$root/build/release"
	exit "$status"
}
